## Build outputs

This folder is used for Arduino IDE build artifacts for the ESP32-S3 target.

Notes:
- The previous `v1.3.7` build outputs were removed from the `v1.3.8` bundle to avoid confusion.
- Rebuild `arduino/DeepCoB_Ezmaker_v1.3.8/DeepCoB_Ezmaker_v1.3.8.ino` to regenerate the `.bin/.elf/.map` files.

